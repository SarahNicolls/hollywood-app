module.exports = [
  require("./createActor"),
  require("./fetchActor"),
  require("./fetchAllActors"),
  require("./removeActor"),
  require("./updateActor")
];

// exporting all files we need to use elsewhere
